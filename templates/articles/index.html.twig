{% extends 'layouts/public.html.twig' %}

{% block title %}Articles ({{ articles|length }}){% endblock %}

{% block content %}
    <div class="row">
        <div class="col-md-12">
            <h1 class="my-4">Articles</h1>
                <div class="row">
                    <div class="col-md-12">
                        <h4>Filter by Categories</h4>
                    </div>
                    <div class="col-md-12">
                        {{ form_start(categoriesForm, {'attr': {'novalidate': 'novalidate'}}) }}
                        {{ form_widget(categoriesForm) }}
                        {{ form_end(categoriesForm) }}
                    </div>
                </div>
            {% include '/articles/_articles_table.html.twig' with {'articles': articles} %}
        </div>
    </div>
{% endblock %}
